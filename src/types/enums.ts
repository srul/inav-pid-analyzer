export enum AnalysisMode {
  LOG_ONLY = "LOG_ONLY",
  SCREENSHOT_ONLY = "SCREENSHOT_ONLY",
  COMBINED = "COMBINED"
}

export enum Severity {
  OK = "OK",
  INFO = "INFO",
  WARNING = "WARNING",
  CRITICAL = "CRITICAL"
}